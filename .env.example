# HuggingFace API token — required for Whisper, DeepSeek, and the vision model
HF_TOKEN=your_huggingface_token_here

# Port for local development (default: 3001)
PORT=3001

# Set to "1" on Vercel (handled automatically by Vercel's runtime)
# VERCEL=1
